/**
 * Notification routes for LagoTrade API
 */

const express = require('express');
const router = express.Router();

// Placeholder route - replace with actual notification functionality
router.get('/', (req, res) => {
    res.json({ message: 'Notification API endpoints' });
});

module.exports = router; 