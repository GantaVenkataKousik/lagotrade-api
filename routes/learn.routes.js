/**
 * Learning resources routes for LagoTrade API
 */

const express = require('express');
const router = express.Router();

// Placeholder route - replace with actual learning functionality
router.get('/', (req, res) => {
    res.json({ message: 'Learning API endpoints' });
});

module.exports = router; 