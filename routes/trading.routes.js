/**
 * Trading routes for LagoTrade API
 */

const express = require('express');
const router = express.Router();

// Placeholder route - replace with actual trading functionality
router.get('/', (req, res) => {
    res.json({ message: 'Trading API endpoints' });
});

module.exports = router; 