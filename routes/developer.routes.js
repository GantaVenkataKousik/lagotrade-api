/**
 * Developer routes for LagoTrade API
 */

const express = require('express');
const router = express.Router();

// Placeholder route - replace with actual developer functionality
router.get('/', (req, res) => {
    res.json({ message: 'Developer API endpoints' });
});

module.exports = router; 