/**
 * Auto Trading routes for LagoTrade API
 */

const express = require('express');
const router = express.Router();

// Placeholder route - replace with actual auto trading functionality
router.get('/', (req, res) => {
    res.json({ message: 'Auto Trading API endpoints' });
});

module.exports = router; 